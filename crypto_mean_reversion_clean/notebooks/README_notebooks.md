Use notebooks only for exploration.

Recommended workflow:
1. Edit production logic in `src/crypto_mr/*.py`.
2. Run the app with `streamlit run app.py`.
3. Use notebooks only to test ideas before moving the code into `src/crypto_mr/`.
